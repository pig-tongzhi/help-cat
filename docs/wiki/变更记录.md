# 变更记录

## 2026-09-24：欢迎页加上「新手做的，欢迎拍砖」

状态：已上线，随 release `20260924-welcome-note-r14`，欢迎页版本 `20260922-welcome-r6`。

- 首屏在「需要的帮助」标签下面加了一句实话：**这版是我业余时间搭的，做得挺糙：功能还在补，页面也还在改。有经验的朋友欢迎指点两句，做网站、做产品、做救助的都行。** 左边一道细橙线，视觉上比正文轻，不抢「进入帮帮小猫」的主按钮。
- 留言表单下面加了一块 **「新手做的，欢迎拍砖」**：说明站点现状，并列出三类具体想要的意见（做网站的看结构/交互/代码、做过救助的说真实流程里最缺什么、路过的说哪里看不懂），收尾一句「不用客气，说得越直接越有用。」虚线边、浅米底，和正文卡片区分开。
- 这块里的「把想法写在这里」和首屏的「留个联系方式」共用同一个跳转（改成按 `data-jump-to-form` 绑定，不再只认一个 id），点击滚到留言表单并聚焦联系方式输入框。
- 新增契约：文案、两个按钮的绑定方式、以及 `.hero-note` / `.feedback-card` / `.feedback-asks` 必须有样式，避免以后只留文案没样式。
## 2026-09-24：77 故事的照片修正 + 素材缺失不再静默

状态：已上线，release 为 `/opt/help-cat/releases/20260924-story-fix-r13`。

- **77 故事第 6 章那张照片换成 77 的正脸照**。原来的 `resting.webp` 是一张下巴+爪子的极近特写，白毛边缘和卡片的奶油底色连成一片，在手机上看就是"图片没加载出来"；它和另外两张（初见/长大）一样，都是从原图直接裁下来的特写，没有做过构图。现在第 6 章用从首页主图派生的 77 正脸竖版（670×750），文件名也改成如实的 `portrait.webp`。
- **故事图 URL 现在带资源版本号**。以前它们是唯一没有 `?v=` 的静态资源，而站内图片是 7 天长缓存 —— 换图之后老用户还会看到旧图。`story-77.js` 改成从 `version.js` 读当前版本拼 `?v=`，和页面其它资源同一套规则，并补了 JS 测试覆盖「有/无 version.js」两种情况。
- **一次网络抖动不再让照片永久消失**：图片 `error` 时先自动重试一次，只有再失败才显示"重新加载照片"按钮；加载成功会清掉失败态。
- **素材缺失不再静默**：IP 入口原来对 `/help-cat/rescue/**` 统一 `try_files ... /help-cat/rescue/index.html`，缺一张图会返回 **200 + HTML**，页面只是"照片不显示"，极难排查（这次发布漏打包一张未跟踪的 webp 就是被它掩盖的）。现在 `/help-cat/rescue/assets/` 单独一个 location，缺失就是 404，并且**不给 404 加长缓存**；nginx 配置已备份后 reload 并验证。
- **探活补上关键素材**：`scripts/health_check.sh` 逐个检查首页主图、三张故事图、站点图标、样式表和故事脚本，要求 200 且 `Content-Type` 是对的类型（回退成 HTML 会被抓住）。服务器上现在 16 项全通过。
- 版本号随之上到 `20260923-product-r5`（欢迎页 `20260922-welcome-r5`）。
## 2026-09-24：隐藏档案不外泄 + 演示猫档案

状态：已上线，release 为 `/opt/help-cat/releases/20260924-showcase-r10`。

- **公开可见性矩阵有测试了**：公开猫咪列表本来就只返回「已审核 + 已发布 + 非 QA」且所在小区 ACTIVE，但这条规则没有测试守着。新增 `server/tests/test_cat_visibility_matrix.py`：待审核 / 未通过 / 隐藏 / 归档 / QA 五种状态都不出现在公开列表，按 id 直查时间线返回 `404 cat_not_found`（不泄露「这条记录存在」），管理员会话仍能看到全部六种状态。
- **演示猫档案**：原先公开页面只有 4 只猫（其中两只都叫「大黑」），其余是 QA 脚手架。新增 8 只带照片、名字正常的档案（汤圆/花卷/灰灰/年糕/豆花/墨镜/虎子/大橘），分配到三个 ACTIVE 小区，公开列表现在 12 只且全部有图。
- 照片来源：这个环境没有图像生成能力（没有文生图 provider，本机 ollama 只有 embedding 模型），因此改用 **CC0 / 公有领域**的真实猫照作为占位图，作者与许可逐个记录在 `scripts/showcase_photos/SOURCES.md`；抓取脚本 `scripts/fetch_showcase_photos.py` 会按站内 4:5 猫卡比例预先裁切（横图交给 CSS 裁会切到猫脸，实测踩过一次）。**这批是占位内容，正式宣传前要换成真实照片**，一条命令可整体收回。
- 线上库内仍有 6 只 2026-08-01 的 QA 猫咪（`[QA-20260801] ...`），对普通用户不可见；彻底清理需要当初的 manifest 走 `help_cat_qa_cleanup.py`。
- **清掉测试残留**：逐个核对公开档案的头像之后，删除 8 条不该出现的档案 —— 2 条头像不是猫（`大黑` 那张是阿据船舶备件后台的截图，`小黑` 那张是夜晚马路的照片）、6 条 2026-08-01 的 QA fixture（其中"管理员录入白猫"的照片是个 19 字节的坏 jpeg）。同时把 `三花` 从已归档的 QA 小区移到 `银湖街道`，它的照片是真猫，之前在任何公开入口都看不到。删除用 `scripts/purge_test_cats.py`：id 逐个写明、默认预览、`--execute` 才落库、重跑 no-op、留 PURGE/REASSIGN 审计行。公开列表因此变成 12 只、全部有照片，重复的「大黑」也随之消失。

## 2026-09-24：架构加固（P0 收尾 + P1 全部）

状态：本地验证通过（Python 266 项全绿、无 deselect），随后发布到云机器。

- **拆掉后端单体**：`server/helpcat/app.py` 从 1780 行降到 76 行，只剩「建配置/引擎 → 装中间件与异常处理 → 挂 router」。58 条路由按领域拆到 `server/helpcat/routers/`（cats / feeding / tasks / communities / admin / auth_routes / impact / leads / media / public_profiles / health），共享的序列化、游标分页、图片处理和日期距离规则分别落到 `serializers` / `pagination` / `media` / `domain`，会话与当前用户走 `dependencies`。**行为等价性**由 123 个 API 测试、逐字一致的 OpenAPI 文档、以及「同方法下无字面量/参数段竞争路由」的检查共同保证。
- **Schema 单一来源**：原先写在 `ensure_schema` 里的 23 条裸 ALTER 收敛为迁移 `011_bootstrap_parity`，运行期兜底与迁移共用 `schema_upgrades.py` 一份定义。顺带修掉两个真 bug：`001_initial` 用 `Base.metadata.create_all` 建当时全部的表，导致 006/007/008/010 的 `create_table` 必然撞 "table already exists"，从空库跑 `alembic upgrade head` 根本跑不完；`006_impact_events` 漏建了模型声明的 `ix_impact_events_created_by`（由新的 parity 测试抓出）。CI 里那条 `--deselect` 已删除，`tests/test_commercial_migrations.py` 里两处自身缺陷（写死 head 版本、`PRAGMA foreign_key_list` 取错列）同时修正。
- **探活与告警**：新增 `GET /api/v1/health/ready`（真跑一次 `SELECT 1`，数据库不可用时回 503 —— 只探进程的 liveness 看不到这种降级）；`scripts/health_check.sh` 检查 readiness、两套静态入口、systemd 单元、`nginx -t` 与数据库盘剩余空间，失败时退出码非 0 并可 POST 企业微信/飞书/Slack webhook；`deploy/systemd/help-cat-healthcheck.{service,timer}` 每 5 分钟跑一次。**外部 uptime 监控仍需注册账号后指向 readiness 地址。**
- **备份**：`scripts/backup_offsite.sh` 用 sqlite3 在线备份 API 做 WAL 安全快照（直接 `cp` 会拿到坏副本）、打包上传目录、写 `META.txt`/`SHA256SUMS`、`--verify` 自证可恢复、按保留期清理，并支持 rclone / coscmd / aws s3 / 自定义命令推异地；`deploy/systemd/help-cat-backup.{service,timer}` 每天 03:20 执行。**异地目标需要在服务器 `/etc/help-cat/backup.env` 里填，否则只落本盘并会明确告警。**
- **日志轮转**：`deploy/logrotate/help-cat`（nginx + 探活日志，14 天）与 `deploy/journald/help-cat.conf`（`SystemMaxUse=500M`）。
- **版本号脚本化**：`scripts/bump_version.sh --next/--check/--dry-run` 管住主版本与欢迎页两条版本线的全部落点，漏升不再靠人记。
- **运维资产入库**：`deploy/nginx/helpcat.conf` 与 `deploy/systemd/help-cat.service` 从服务器原样归档，重建或迁移时不必靠回忆。
- 新增测试：`server/tests/test_schema_single_source.py`、`tests/test_app_split_contract.py`、`tests/test_bump_version_script.py`、`tests/test_ops_scripts.py`。

## 2026-08-12：确认稿首页与四项真实成果指标

状态：已上线，生产 release 为 `/opt/help-cat/releases/20260812-reference-home-r1`。

- 首页按确认稿重做为 77 暖白编辑式首屏，桌面使用 50/50 构图、四项横排成果、四张真实猫咪档案与单张救助任务；手机使用独立 77 裁切、四项单行成果、两列档案和固定四项底栏。
- 公开成果口径改为“已救助、找到新家、医疗救助、爱心支持”。数据来自可审计、可冲销的 `impact_events` 台账；未录入或接口失败时诚实显示 0，不使用虚构数字。
- 新增迁移 `006_impact_events` 与管理员创建、分页、冲销接口；QA 数据不会进入公开汇总。
- 77 档案已审核并公开，档案编号 `HC-29DA0FB2`，归属“银湖街道”，公开故事键为 `story-77`。
- 发布前后执行 H5 合同 24 项、后端 35 项、总测试 51 项（1 个环境性跳过）、Node 行为测试及全部 JavaScript 语法检查；生产数据库副本迁移、生产迁移、完整性、API health、Nginx 和域名页面版本均通过。
- 发布备份位于 `/opt/help-cat/backups/20260812-reference-home-r1`；前一 release 路径记录在该目录的 `PREVIOUS_RELEASE`。

## 2026-08-11：77 编辑叙事 H5（发布前已完成，等待生产授权）

状态：发布前验证、文档和分支推送已完成；尚未备份、部署、导入 77 档案或切换任何生产资源。

- H5 首页新增 77 编辑式首屏，分享路由固定为 `https://helpcat.xyz/#story-77`，版本标记为 `20260811-77-editorial-r1`。
- 品牌源为 `app/rescue/assets/brand/helpcat-77-mark.svg`；浏览器 favicon 为同几何的 `app/rescue/assets/brand/favicon.svg`，缓存刷新后应显示黑眼斑标志。
- 77 图片仅使用 `app/rescue/assets/77/` 中已经去除元数据的公开衍生图；叙事与导入 payload 不写精确位置、联系方式或未确认的健康/医疗信息。
- 本地在 390×844、768×1024、1024×768、1440×900 完成截图和交互检查：首屏双眼及黑眼斑未同时被裁切、无横向滚动/文字裁切/底部导航重叠；`#story-77` 可直达，浏览器 Back 返回首页，两个故事 CTA 分别进入猫咪档案和建档登录门槛。
- SUPER_ADMIN 后台入口与相册选择器以静态/合同方式复核：`SUPER_ADMIN` 被视为管理角色并显示“进入管理后台”；`#cat-photo-file` 为 `type=file`、`accept=image/*` 且没有 `capture`，由系统选择相册或相机，不强制后置相机。
- 本机实际执行 `python3 -m unittest discover -s server -v`（30 通过）、`python3 -m unittest discover -s tests -v`（47 通过、1 个缺少本机 Alembic 的既有跳过）、`node --test tests/js/*.test.js`（20 通过）、全量 JS `node --check` 与 `git diff --check`（均零退出）。顶层默认 `python3 -m unittest discover -v` 因目录不以 `test*` 命名而发现 0 项；已以 `server` 和 `tests` 两个真实测试根补足。
- 生产获授权后必须先建立带时间戳的 release、H5/admin/API、数据库及上传媒体元数据备份，再以新 release 目录和原子 symlink/Nginx root 切换；任何健康、登录、后台入口、77 路由、媒体或布局验收失败，立即恢复前一 release 与匹配数据/媒体备份。

77 档案导入（仅获生产授权并先确认 ACTIVE 小区 ID 后执行；密码只从环境读取，示例不含凭据）：

```bash
python3 /opt/help-cat/releases/<timestamp>/scripts/help_cat_77_seed.py \
  --base-url https://helpcat.xyz \
  --username '<ADMIN_USERNAME>' \
  --password-env HELP_CAT_77_PASSWORD \
  --community-id '<ACTIVE_COMMUNITY_ID>' \
  --photo /opt/help-cat/releases/<timestamp>/rescue/assets/77/rescue-day.webp
```

脚本只调用原子接口 `/api/v1/admin/cat-drafts/import`。首次输出记录 `cat_id`、`media_id` 与 `changed`，终态必须是 `PENDING_REVIEW + HIDDEN`；复跑使用固定幂等键并返回同一档案且 `changed: false`。导入不会审核或公开，操作者必须在管理后台人工核对照片、社区与字段，再分别执行审核和公开。

## 2026-08-02：高级萌宠社区 UI 与小区候选联审

状态：已上线（2026-08-10 至 2026-08-11 完成生产发布与域名验收）。

- H5 视觉升级为暖白、深墨、杏色与雾紫/薄荷点缀的高级萌宠社区风格，保留真实猫咪照片优先。
- 管理后台同步高级视觉与手机/平板/桌面响应式布局。
- 双端底部加入“安得广厦千万间，大庇天下小猫俱欢颜”。
- 后台左上猫头改为 `/help-cat/rescue/index.html#home`，不在 URL 中传递 Token。
- 猫咪建档第二步支持“选择已有小区/填写新小区”双模式。
- 新候选小区与猫咪在同一事务创建；失败不留下孤立数据。
- 增加 `PENDING_REVIEW / NEEDS_CHANGES / ACTIVE / MERGED / REJECTED / ARCHIVED` 候选治理状态。
- 创建者可修正本人待审/待补充候选；管理员和超级管理员可核准、退回、合并或驳回无效内容。
- 合并会事务改挂所有关联猫咪并记录审计；猫咪在小区 ACTIVE 前不能公开。
- 增加 `version` 乐观锁，过期编辑/审核/合并返回 HTTP 409。
- `version` 覆盖候选编辑/审核/合并/归档和单只猫咪改挂；过期写入返回 HTTP 409。
- 候选被驳回、隐藏或归档后，管理员可搜索全部 ACTIVE 小区并将关联猫咪改挂后继续审核。
- 猫咪、小区、任务、本人提交和用户列表使用稳定游标分页，默认 24、最大 100。
- 后台联审使用服务端关联总数和最多 3 只预览，合并目标通过服务端搜索，不受当前 24 条页面限制。
- 猫咪 payload 直接携带小区名称/街道，跨页加载不会退化成“社区范围内”。
- H5 猫咪建档增加用户作用域 `Idempotency-Key`；丢包重试不重复创建、不重复扣配额。
- 新增数据库级有效小区规范名唯一约束、猫咪幂等唯一约束和合并目标自外键。
- 新增 `002_community_candidates`、`003_scale_integrity` 迁移及旧 SQLite 安全回填/碰撞归并。
- Alembic 显式读取 `HELPCAT_DATABASE_URL`，避免正式部署误迁移默认相对路径。
- 保留旧审核 payload 到历史 HIDDEN 状态的滚动发布兼容。
- 修复公开/管理员双视图对已撤销管理员 Session 的识别，退出后的旧 Token 不能读取待审猫咪。
- 生产副本迁移演练：001 → 002 → 003；六类数据行数与核心 ID 哈希不变，完整性 `ok`、外键违规 0、孤儿猫咪 0。
- 当前门禁：Python 64 项、Node 17 项，共 81 项；macOS 系统 Python 因未安装 Alembic 跳过 1 项真实迁移用例，该用例已在生产虚拟环境和生产数据库副本上通过。最终发布记录还会补充生产入口验收结果。

生产发布与域名：

- Git 提交：`b1babca`（分支 `agent/mobile-upload-admin-entry`，已推送 GitHub）。
- 生产 release：`/opt/help-cat/releases/20260810-helpcat-community-domain-r4`。
- 生产备份：`/opt/help-cat/backups/20260810-2317-community-domain-r4`；SQLite 备份完整性为 `ok`。
- 发布包 SHA-256：`ab6379d1f574fe980bb5f77ff870c860cece5dda00b32118dd55b6a415397507`。
- 正式入口：`https://helpcat.xyz/`；后台：`https://helpcat.xyz/admin/`。
- `helpcat.xyz` 与 `www.helpcat.xyz` 的 A 记录均指向 `175.178.41.19`。
- Let's Encrypt ECDSA 证书覆盖两个域名，有效期至 2026-11-08；`certbot-renew.timer` 已启用自动续期。
- 腾讯云轻量应用服务器防火墙已增加来源 `0.0.0.0/0`、TCP 443 入站规则；HTTP 自动 301 跳转 HTTPS。
- 域名 `/api/` 与兼容路径 `/help-cat-api/` 均通过 HTTPS 转发到 Help Cat API。
- 线上验收：根域名、www、后台、两套 API 与旧 IP 入口均返回 200；TLS 校验结果 0；手机 390px、平板 820px 均无横向溢出；Nginx 配置、服务状态、数据库 `003_scale_integrity` 与完整性检查全部通过。
- 回滚方式：恢复上述备份目录内的 SQLite、release、H5、后台和 Nginx 配置；仅需重启 `help-cat.service` 并在 `nginx -t` 成功后 reload，不影响同机其他服务。

相关设计：`docs/superpowers/specs/2026-08-02-premium-pet-community-linked-review-design.md`

相关计划：`docs/superpowers/plans/2026-08-02-premium-pet-community-linked-review.md`

域名发布计划：`docs/superpowers/plans/2026-08-10-help-cat-domain-https-release.md`

## 2026-08-02：角色、小区流程、版本提示与 Wiki

状态：已上线。

- 现场确认生产数据库中唯一超级管理员角色正确。
- 定位“超级管理员显示为志愿者”为已打开旧页面继续运行部署前脚本。
- 刷新后确认超级管理员文案和后台入口出现。
- 角色判断增加规范化，未知角色按 USER 最小权限处理。
- 创建猫咪第二步增加“没有找到小区？”快捷入口。
- H5 小区表单根据 USER/ADMIN/SUPER_ADMIN 显示不同文案。
- 修复 SUPER_ADMIN 创建小区后误提示等待审核。
- 增加非破坏性在线版本更新提示。
- 建立详细 GitHub Wiki 文档源。
- 全量自动化回归通过 42 项（Python 36 项、Node 行为测试 6 项）。
- 生产 HTML 增加禁止缓存响应头，避免已打开页面长期运行旧脚本。
- 管理后台退出会撤销服务端 Session，并同时清除后台与 H5 的同源 Token。
- 版本探测拒绝非 2xx 响应，发现新版本后提示会保持到用户刷新。
- 增加可执行行为测试，覆盖退出成功、网络失败、防重复点击、同版本、新版本和错误响应。

生产版本：`20260802-030121-community-quality-r3`

生产 release：`/opt/help-cat/releases/20260802-030121-community-quality-r3`

生产备份：`/opt/help-cat/backups/20260802-030121-community-quality-r3`

相关设计：`docs/superpowers/specs/2026-08-02-help-cat-role-community-wiki-quality-design.md`

相关计划：

- `docs/superpowers/plans/2026-08-02-help-cat-role-community-quality.md`
- `docs/superpowers/plans/2026-08-02-help-cat-github-wiki.md`

## 2026-08-02：手机图片和管理后台入口

- 移除 H5 图片 input 的 `capture="environment"`，不再强制打开后置相机。
- 保留 `accept="image/*"`，由手机系统提供相册/相机选择。
- ADMIN 和 SUPER_ADMIN 在“我的”页显示“进入管理后台”。
- 同源传递当前 Token，后台重新验证角色。
- 更新响应式账号操作按钮。

GitHub：<https://github.com/pig-tongzhi/help-cat/pull/1>

生产备份：`/opt/help-cat/backups/20260802-014229-mobile-admin`

## 2026-08-01：响应式猫咪卡片和 QA 数据

- 修复破损图片导致的大面积空白卡片。
- 增加永久占位图、健康状态和审核状态样式。
- 优化手机、平板和桌面卡片布局。
- 建立可重复生成和安全清理的 QA 数据集。
- 覆盖小区、猫咪、任务、角色和图片异常状态。

## 2026-07-31：真实后端、企业化 H5 和超级管理员

- H5 从演示 localStorage 数据切换到真实 FastAPI API。
- 增加用户名密码注册、登录、会话恢复和退出。
- 增加猫咪三步建档、图片上传、定位回退和每日配额。
- 建立独立管理后台。
- 增加唯一 SUPER_ADMIN 及普通管理员赋权。
- 建立猫咪、小区、任务、媒体和审计数据模型。
- 部署 systemd Uvicorn 服务和 Nginx 反向代理。

## 2026-07-26：社区协作和商业化架构设计

- 形成社区猫、小区、救助任务和审核治理模型。
- 创建微信小程序代码骨架。
- 建立 Dockerfile、依赖和 Alembic 迁移结构。

## 2026-07-25：项目启动

- 完成杭州流浪猫救助产品调研和首版设计。
- 建立 H5 页面、猫咪档案和社区救助基础体验。

## 维护要求

每次生产发布至少记录：

- 日期和版本标记。
- 用户可见变更。
- 数据库或权限变更。
- 测试结果。
- release 与 backup 路径。
- GitHub PR/提交。
- 已知限制和回滚方式。
