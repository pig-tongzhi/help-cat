# /etc/systemd/journald.conf.d/help-cat.conf
#
# 不设上限时 journald 会一直长到吃满 /var/log/journal（这台机器只有 40G，还要放
# 数据库和上传目录）。500M + 1 个月对当前的量级足够回溯，重启一次 journald 生效。
[Journal]
SystemMaxUse=500M
SystemKeepFree=1G
MaxRetentionSec=1month
